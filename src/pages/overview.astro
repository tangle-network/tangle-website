---
import BaseLayout from '../layouts/BaseLayout.astro';
import StatsStrip from '../components/ui/StatsStrip.astro';
import SectionHead from '../components/ui/SectionHead.astro';
import FeatureCard from '../components/ui/FeatureCard.astro';
import Hero from '../components/ui/Hero.astro';
import Section from '../components/ui/Section.astro';
import CtaBlock from '../components/ui/CtaBlock.astro';

const iconProfile = '<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>';
const iconSandbox = '<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M9 3v18"/><path d="M3 9h6"/></svg>';
const iconData = '<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M3 5v14a9 3 0 0 0 18 0V5"/><path d="M3 12a9 3 0 0 0 18 0"/></svg>';
const iconEvals = '<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>';

const blueprints = [
  {
    name: 'Arbitrum Orbit with Avail DA',
    href: 'https://github.com/tangle-network/avail-arbitrum-orbit-raas',
    logo: '/images/webflow/Avail Vault.png',
    image: '/images/webflow/effect2.png',
    description: 'Deploy your own Arbitrum Orbit L2 chain using Avail\'s data availability layer.',
  },
  {
    name: 'Envio Indexer',
    href: 'https://github.com/tangle-network/envio-indexer-blueprint',
    logo: '/images/webflow/envio_indexer_logo.jpeg',
    image: '/images/webflow/effect2.png',
    description: 'A Tangle Blueprint for running envio indexers on any supported chain. Envio-as-a-service for Tangle.',
  },
  {
    name: 'Espresso RaaS',
    href: 'https://github.com/tangle-network/espresso-raas-blueprint',
    logo: '/images/webflow/tangle icon.png',
    image: '/images/webflow/effect.png',
    description: 'A Tangle Blueprint to deploy Arbitrum Orbit Rollups over Espresso Network.',
  },
  {
    name: 'FROST Multi-party Computation',
    href: 'https://github.com/tangle-network/frost-blueprint',
    logo: '/images/webflow/tangle icon.png',
    image: '/images/webflow/effect.png',
    description: 'Leverage FROST (Flexible Round-Optimized Schnorr Threshold Signatures) in your services by composing blueprints together.',
  },
  {
    name: 'Gaia AI Agent',
    href: 'https://www.gaianet.ai/',
    logo: '/images/webflow/GaiaNet-1024x608.jpg',
    image: '/images/webflow/GaiaNet-1024x608.jpg',
    description: 'On-demand AI agents for chat based applications, image generation, and anything else.',
  },
  {
    name: 'Hello World',
    href: '#',
    logo: '/images/webflow/tangle icon.png',
    image: '/images/webflow/effect2.png',
    description: 'Get started with our most basic Hello World service.',
  },
  {
    name: 'Hyperbridge Relayer',
    href: 'https://hyperbridge.network/',
    logo: '/images/webflow/tangle icon.png',
    image: '/images/webflow/effect.png',
    description: 'Instance-able Hyperbridge relayers for cross-chain message transmission.',
  },
  {
    name: 'Hyperlane Relayer',
    href: 'https://hyperlane.xyz/',
    logo: '/images/webflow/Hyperlane.png',
    image: '/images/webflow/effect2.png',
    description: 'Run Hyperlane relayers with Tangle integration. Automated dev ops included.',
  },
  {
    name: 'LayerZero DVN',
    href: 'https://layerzero.network/',
    logo: '/images/webflow/layerzero.jpeg',
    image: '/images/webflow/effect.png',
    description: 'Create LayerZero Decentralized Verifier Networks (DVN) on Tangle for cross-chain messaging.',
  },
  {
    name: 'Obol DVT',
    href: 'https://obol.org/',
    logo: '/images/webflow/obol.png',
    image: '/images/webflow/effect2.png',
    description: 'Deploy Obol Distributed Validator Clusters on Tangle for liquid staking.',
  },
  {
    name: 'TLS Notary',
    href: 'https://tlsnotary.org/',
    logo: '/images/webflow/tangle icon.png',
    image: '/images/webflow/effect.png',
    description: 'Build TLSNotary-backed services: notaries, proofs, verification.',
  },
  {
    name: 'Typescript',
    href: '#',
    logo: '/images/webflow/tangle icon.png',
    image: '/images/webflow/effect2.png',
    description: 'Build services in TypeScript or other languages via SDK extensions.',
  },
];

const partnerLogos = [
  { name: 'Biconomy', src: '/images/webflow/biconomy.png' },
  { name: 'Youbi Capital', src: '/images/webflow/Youbi Capital.png' },
  { name: 'Lemniscap', src: '/images/webflow/Lemniscap.png' },
  { name: 'Webb', src: '/images/webflow/webb.png' },
  { name: 'EigenLayer', src: '/images/webflow/EigenLayer.png' },
  { name: 'Polychain Capital', src: '/images/webflow/Polychain Capital.png' },
  { name: 'Level', src: '/images/webflow/level.png' },
  { name: 'Hyperlane', src: '/images/webflow/Hyperlane.png' },
];
---

<BaseLayout title="Tangle Protocol — the substrate" description="The protocol layer behind tangle.tools — operator network, staking, x402 settlement. The infrastructure substrate Blueprints run on.">

  <style>
    .ov-surfaces-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 16px;
      margin-top: 2.5rem;
    }
    @media (max-width: 1100px) {
      .ov-surfaces-grid { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 640px) {
      .ov-surfaces-grid { grid-template-columns: 1fr; }
    }

    /* HOW IT FLOWS — data-flow diagram */
    .dflow {
      position: relative;
      max-width: 64rem;
      margin: 3rem auto 0;
      display: grid;
      grid-template-columns: 1fr;
      grid-template-rows: auto 220px auto auto;
      gap: 0;
    }
    .dflow-cols {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      position: relative;
      z-index: 2;
    }
    .dflow-col {
      background: var(--depth-2);
      border: 1px solid var(--border-default);
      border-radius: 14px;
      padding: 1.25rem 1.25rem 1rem;
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
      min-height: 14rem;
    }
    .dflow-col[data-source="public"] { border-color: rgba(99,102,241,0.3); }
    .dflow-col[data-source="personal"] { border-color: rgba(52,211,153,0.3); }
    .dflow-col[data-source="licensed"] { border-color: rgba(255,184,0,0.3); }
    .dflow-col-head { display: flex; flex-direction: column; gap: 0.2rem; padding-bottom: 0.5rem; border-bottom: 1px dashed rgba(165,170,252,0.2); }
    .dflow-col-label { font-family: var(--font-mono); font-size: 0.72rem; font-weight: 700; letter-spacing: 0.16em; color: var(--brand-cool); }
    .dflow-col[data-source="personal"] .dflow-col-label { color: #34D399; }
    .dflow-col[data-source="licensed"] .dflow-col-label { color: #FFB800; }
    .dflow-col-sub { font-family: var(--font-mono); font-size: 0.7rem; color: var(--text-muted); letter-spacing: 0.04em; }
    .dflow-list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 0.4rem; font-family: var(--font-mono); font-size: 0.85rem; color: var(--text-primary); }
    .dflow-list li { padding-left: 0.85rem; position: relative; }
    .dflow-list li::before { content: '·'; position: absolute; left: 0; color: var(--brand-cool); font-weight: 700; }
    .dflow-col-meta { margin-top: auto; padding-top: 0.6rem; font-family: var(--font-mono); font-size: 0.68rem; color: var(--text-muted); letter-spacing: 0.04em; border-top: 1px solid var(--border-subtle); }

    .dflow-svg {
      grid-column: 1 / -1;
      width: 100%;
      height: 220px;
      margin: -10px 0 -10px;
      pointer-events: none;
      position: relative;
      z-index: 1;
    }
    .dflow-beam {
      stroke-dashoffset: 800;
      animation: dflow-draw 1.6s cubic-bezier(0.6,0.05,0.4,1) forwards;
      opacity: 0;
    }
    .dflow.is-in-view .dflow-beam {
      opacity: 1;
      animation-play-state: running;
    }
    .dflow-beam-out {
      animation-delay: 0.7s;
    }
    @keyframes dflow-draw {
      from { stroke-dashoffset: 800; opacity: 0; }
      to   { stroke-dashoffset: 0;   opacity: 1; }
    }

    .dflow-mid { display: flex; justify-content: center; }
    .dflow-window {
      width: min(100%, 38rem);
      background: linear-gradient(135deg, rgba(99,102,241,0.06), rgba(165,170,252,0.04));
      border: 1px solid rgba(99,102,241,0.3);
      border-radius: 14px;
      padding: 1.25rem 1.5rem;
      box-shadow: 0 10px 40px rgba(99,102,241,0.1);
    }
    .dflow-window-head { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; margin-bottom: 0.85rem; padding-bottom: 0.5rem; border-bottom: 1px dashed rgba(165,170,252,0.22); }
    .dflow-window-label { font-family: var(--font-mono); font-size: 0.72rem; font-weight: 700; letter-spacing: 0.18em; color: #A5AAFC; }
    .dflow-window-meta { font-family: var(--font-mono); font-size: 0.7rem; color: var(--text-muted); }
    .dflow-window-bars { display: flex; flex-direction: column; gap: 0.55rem; }
    .dflow-window-bar { display: grid; grid-template-columns: 5rem 1fr 3rem; gap: 0.85rem; align-items: center; font-family: var(--font-mono); font-size: 0.8rem; color: var(--text-secondary); }
    .dflow-window-bar-key { color: var(--text-muted); letter-spacing: 0.04em; }
    .dflow-window-bar-track { display: block; height: 6px; background: rgba(255,255,255,0.04); border-radius: 3px; overflow: hidden; }
    .dflow-window-bar-track > span { display: block; height: 100%; border-radius: 3px; }
    .dflow-window-bar-num { color: var(--text-primary); font-weight: 700; text-align: right; }

    .dflow-bottom { display: flex; justify-content: center; margin-top: 2.5rem; }
    .dflow-eval {
      width: min(100%, 36rem);
      background: var(--depth-2);
      border: 1px solid rgba(52,211,153,0.3);
      border-radius: 14px;
      padding: 1.25rem 1.5rem;
    }
    .dflow-eval-head { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; margin-bottom: 0.85rem; padding-bottom: 0.5rem; border-bottom: 1px dashed rgba(52,211,153,0.22); }
    .dflow-eval-label { font-family: var(--font-mono); font-size: 0.72rem; font-weight: 700; letter-spacing: 0.18em; color: #34D399; }
    .dflow-eval-meta { font-family: var(--font-mono); font-size: 0.7rem; color: var(--text-muted); }
    .dflow-eval-rows { display: flex; flex-direction: column; gap: 0.55rem; }
    .dflow-eval-row { display: grid; grid-template-columns: 7rem 1fr 2.5rem; gap: 0.85rem; align-items: center; font-family: var(--font-mono); font-size: 0.75rem; color: var(--text-secondary); }
    .dflow-eval-bar { display: block; height: 6px; background: rgba(255,255,255,0.04); border-radius: 3px; overflow: hidden; }
    .dflow-eval-bar > span { display: block; height: 100%; background: linear-gradient(90deg, #34D399, #6EE7B7); border-radius: 3px; }
    .dflow-eval-num { color: #34D399; font-weight: 700; text-align: right; font-family: var(--font-mono); }

    @media (max-width: 768px) {
      .dflow-cols { grid-template-columns: 1fr; }
      .dflow-svg { display: none; }
      .dflow-mid { margin-top: 1.5rem; }
    }
    @media (prefers-reduced-motion: reduce) {
      .dflow-beam { animation: none !important; opacity: 1 !important; stroke-dashoffset: 0 !important; }
    }
  </style>

  <script>
    // Activate diagram beams when the section enters viewport.
    (() => {
      const flow = document.querySelector('.dflow');
      if (!flow) return;
      if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
        flow.classList.add('is-in-view');
        return;
      }
      const io = new IntersectionObserver((entries) => {
        for (const e of entries) {
          if (e.isIntersecting) { flow.classList.add('is-in-view'); io.disconnect(); break; }
        }
      }, { threshold: 0.25 });
      io.observe(flow);
    })();
  </script>

  <Hero
    eyebrow="TANGLE PROTOCOL"
    eyebrowColor="brand"
    eyebrowIcon="shield"
    title={`One Blueprint.<br/>Many <span class="wf-gradient-text">operator instances.</span>`}
    subtitle="The crypto-economic substrate underneath every Blueprint — operator instancing, staking-backed SLAs, slashing, x402 settlement on every call."
    lede="Developers ship a Blueprint once. The protocol runs it on a network of staked operators, settles per-call payouts in any asset, and slashes operators that miss SLAs. No infra, no SLA, no on-call."
    primaryCta={{ label: 'Explore Docs', href: 'https://docs.tangle.tools', external: true }}
    secondaryCta={{ label: 'View Stake', href: '/stake' }}
    layout="one-col"
    align="left"
    videoBg={{
      poster: '/images/hero/abstract-objects-poster.jpg',
      webm: '/videos/abstract-objects.webm',
      mp4: '/videos/abstract-objects.mp4',
    }}
  />

  <StatsStrip
    items={[
      { label: '32+ Blueprints in production', icon: 'layers' },
      { label: '200+ operator instances running', icon: 'globe' },
      { label: 'Staking-backed SLAs', icon: 'shield' },
      { label: 'x402 settlement on every call', icon: 'dollar' },
    ]}
  />

  <!-- Partners -->
  <div class="wf-partners">
    <div class="wf-partners-heading">Partners &amp; Backers</div>
    <div class="wf-marquee">
      <div class="wf-marquee-inner">
        {[...partnerLogos, ...partnerLogos].map(({ name, src }) => (
          <img src={src} alt={name} loading="lazy" class="wf-marquee-logo" />
        ))}
      </div>
      <div class="wf-marquee-inner">
        {[...partnerLogos, ...partnerLogos].map(({ name, src }) => (
          <img src={src} alt={name} loading="lazy" class="wf-marquee-logo" />
        ))}
      </div>
    </div>
  </div>

  <!-- Section 2: How it earns -->
  <Section variant="dark" pad="default" maxWidth="wide">
    <div id="section_wrapper_2" style="position: relative;">
      <div style="text-align: center; margin-bottom: 3rem;">
        <div style="font-family: var(--font-mono); font-size: var(--type-mono-eyebrow); color: var(--brand-cool); text-transform: uppercase; letter-spacing: var(--type-mono-eyebrow-track); margin-bottom: 0.75rem;">HOW IT EARNS</div>
        <h2 style="font-family: var(--font-display); font-size: var(--type-h2); font-weight: var(--type-h2-weight); line-height: var(--type-h2-line); letter-spacing: var(--type-h2-track); color: var(--text-primary); margin: 0 0 1rem;">Operators run it. <span class="wf-gradient-text">You collect.</span></h2>
        <p style="font-family: var(--font-sans); font-size: var(--type-body-l); color: var(--text-secondary); max-width: 50rem; margin: 0 auto;">Operators stake to serve. Customers pay per call. Payouts settle in any asset via x402. Operators that miss SLA get slashed.</p>
      </div>

      <div class="wf-builder-feature-grid">
          <!-- Monetize Blueprints Card -->
          <div class="wf-builder-feature-card">
            <div class="wf-builder-feature-top">
              <div class="wf-h4">Monetize Blueprints</div>
              <div class="wf-body-02">Instance-able service templates. Earn while operators run them.</div>
            </div>
            <div class="wf-builder-feature-bottom">
              <div class="wf-builder-ui-block">
                <div class="wf-builder-ui-top">
                  <div class="wf-builder-ui-top-left">
                    <img loading="lazy" src="/images/webflow/icon-2.svg" alt="" class="wf-builder-dock-image" />
                    <div>
                      <div class="wf-builder-ui-top-text">AI&nbsp;Agent Sandbox</div>
                      <div class="wf-builder-ui-author">Author: Tangle Network</div>
                    </div>
                  </div>
                  <div class="wf-builder-ui-replace">Replace</div>
                </div>
                <div class="wf-builder-ui-divider"></div>
                <div class="wf-builder-ui-description">
                  AI&nbsp;agent sandboxes in isolated environments. Add dependencies, SSH in, trigger workflows.
                </div>
                <div class="wf-builder-ui-stats">
                  <div class="wf-builder-ui-stat">
                    <div class="wf-builder-ui-stat-label">Stakers</div>
                    <div class="wf-builder-ui-stat-value">250</div>
                  </div>
                  <div class="wf-builder-ui-stat">
                    <div class="wf-builder-ui-stat-label">Operators</div>
                    <div class="wf-builder-ui-stat-value">50</div>
                  </div>
                  <div class="wf-builder-ui-stat">
                    <div class="wf-builder-ui-stat-label">TVL</div>
                    <div class="wf-builder-ui-stat-value">$1,250,000</div>
                  </div>
                </div>
                <div class="wf-builder-ui-button">
                  <span>Deploy Blueprint</span>
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M9 9V6C9 4.34315 7.65685 3 6 3C4.34315 3 3 4.34315 3 6C3 7.65685 4.34315 9 6 9H9ZM9 9V15M9 9H15M9 15V18C9 19.6569 7.65685 21 6 21C4.34315 21 3 19.6569 3 18C3 16.3431 4.34315 15 6 15H9ZM9 15H15M15 15H18C19.6569 15 21 16.3431 21 18C21 19.6569 19.6569 21 18 21C16.3431 21 15 19.6569 15 18V15ZM15 15V9M15 9V6C15 4.34315 16.3431 3 18 3C19.6569 3 21 4.34315 21 6C21 7.65685 19.6569 9 18 9H15Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                </div>
              </div>
            </div>
          </div>

          <!-- Build with Standards Card -->
          <div class="wf-builder-feature-card">
            <div class="wf-builder-feature-top">
              <div class="wf-h4">Build with Standards</div>
              <div class="wf-body-02">Compose Blueprints from a growing library of reusable primitives.</div>
            </div>
            <div class="wf-builder-feature-bottom">
              <div class="wf-builder-standards-grid">
                <div class="wf-builder-standards-row">
                  <div class="wf-builder-standards-plus">
                    <svg xmlns="http://www.w3.org/2000/svg" width="35" height="35" viewBox="0 0 35 35" fill="none">
                      <rect width="35" height="35" rx="9" fill="currentColor"/>
                      <path d="M25.5 17V18C25.5 18.1326 25.4473 18.2598 25.3536 18.3536C25.2598 18.4473 25.1326 18.5 25 18.5H18.5V25C18.5 25.1326 18.4473 25.2598 18.3536 25.3536C18.2598 25.4473 18.1326 25.5 18 25.5H17C16.8674 25.5 16.7402 25.4473 16.6464 25.3536C16.5527 25.2598 16.5 25.1326 16.5 25V18.5H10C9.86739 18.5 9.74021 18.4473 9.64645 18.3536C9.55268 18.2598 9.5 18.1326 9.5 18V17C9.5 16.725 9.725 16.5 10 16.5H16.5V10C16.5 9.725 16.725 9.5 17 9.5H18C18.1326 9.5 18.2598 9.55268 18.3536 9.64645C18.4473 9.74021 18.5 9.86739 18.5 10V16.5H25C25.275 16.5 25.5 16.725 25.5 17Z" fill="white"/>
                    </svg>
                  </div>
                  <div class="wf-builder-standards-item">
                    <img src="/images/webflow/icon-2.svg" alt="" class="wf-builder-dock-image" />
                    <div class="wf-builder-standards-name">Blueprint SDK</div>
                  </div>
                  <div class="wf-builder-standards-plus">
                    <svg xmlns="http://www.w3.org/2000/svg" width="35" height="35" viewBox="0 0 35 35" fill="none">
                      <rect width="35" height="35" rx="9" fill="currentColor"/>
                      <path d="M25.5 17V18C25.5 18.1326 25.4473 18.2598 25.3536 18.3536C25.2598 18.4473 25.1326 18.5 25 18.5H18.5V25C18.5 25.1326 18.4473 25.2598 18.3536 25.3536C18.2598 25.4473 18.1326 25.5 18 25.5H17C16.8674 25.5 16.7402 25.4473 16.6464 25.3536C16.5527 25.2598 16.5 25.1326 16.5 25V18.5H10C9.86739 18.5 9.74021 18.4473 9.64645 18.3536C9.55268 18.2598 9.5 18.1326 9.5 18V17C9.5 16.725 9.725 16.5 10 16.5H16.5V10C16.5 9.725 16.725 9.5 17 9.5H18C18.1326 9.5 18.2598 9.55268 18.3536 9.64645C18.4473 9.74021 18.5 9.86739 18.5 10V16.5H25C25.275 16.5 25.5 16.725 25.5 17Z" fill="white"/>
                    </svg>
                  </div>
                  <div class="wf-builder-standards-item">
                    <img src="/images/webflow/tangle icon.png" alt="" class="wf-builder-dock-image" />
                    <div class="wf-builder-standards-name">Tangle Primitives</div>
                  </div>
                  <div class="wf-builder-standards-plus">
                    <svg xmlns="http://www.w3.org/2000/svg" width="35" height="35" viewBox="0 0 35 35" fill="none">
                      <rect width="35" height="35" rx="9" fill="currentColor"/>
                      <path d="M25.5 17V18C25.5 18.1326 25.4473 18.2598 25.3536 18.3536C25.2598 18.4473 25.1326 18.5 25 18.5H18.5V25C18.5 25.1326 18.4473 25.2598 18.3536 25.3536C18.2598 25.4473 18.1326 25.5 18 25.5H17C16.8674 25.5 16.7402 25.4473 16.6464 25.3536C16.5527 25.2598 16.5 25.1326 16.5 25V18.5H10C9.86739 18.5 9.74021 18.4473 9.64645 18.3536C9.55268 18.2598 9.5 18.1326 9.5 18V17C9.5 16.725 9.725 16.5 10 16.5H16.5V10C16.5 9.725 16.725 9.5 17 9.5H18C18.1326 9.5 18.2598 9.55268 18.3536 9.64645C18.4473 9.74021 18.5 9.86739 18.5 10V16.5H25C25.275 16.5 25.5 16.725 25.5 17Z" fill="white"/>
                    </svg>
                  </div>
                  <div class="wf-builder-standards-item">
                    <img src="/images/webflow/Hyperlane.png" alt="" class="wf-builder-dock-image" />
                    <div class="wf-builder-standards-name">Hyperlane Relayer</div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Restake & Instances Cards -->
        <div class="wf-builder-feature-grid" style="margin-top: 1rem;">
          <!-- Restake Assets -->
          <div class="wf-builder-feature-card">
            <div class="wf-feature-image">
              <img
                src="/images/webflow/Restake 7.png"
                alt="Restake Assets"
                loading="lazy"
                class="wf-image-76"
              />
            </div>
            <div class="wf-builder-feature-top-3">
              <div class="wf-h4-2">Restake Assets From Any Ecosystem</div>
              <div class="wf-body-02">Permissionless and network-agnostic. Source security from any chain.</div>
            </div>
          </div>

          <!-- Unlock Possibilities -->
          <div class="wf-builder-feature-card">
            <div class="wf-feature-image right">
              <img
                src="/images/webflow/Instances View.png"
                alt="Instances View"
                loading="lazy"
                class="wf-image-76"
              />
            </div>
            <div class="wf-builder-feature-top-3">
              <div class="wf-h4-2">Deploy Blueprint Instances</div>
              <div class="wf-body-02">Spin up any Blueprint instance into your own network or environment.</div>
            </div>
          </div>
        </div>
    </div>
  </Section>

  <!-- CTA Section -->
  <CtaBlock
    title="Build a Blueprint. Earn per operator instance."
    body="Service fees, royalties, protocol rewards — settled on chain."
    primaryCta={{ label: 'Start Building', href: 'https://form.typeform.com/to/IE4hT9jW', external: true }}
  />

  <script is:inline>
    function createGridReveal(sectionId) {
      new p5((p) => {
        const CELL_SIZE = 40;
        const STARTING_STROKE_ALPHA = 1;
        const STARTING_FILL_ALPHA = 0.5;
        const AMT_FADE_PER_FRAME = 5;
        const MAX_CELLS = 150;
        const REVEAL_INTERVAL = 1000;
        const DYNAMIC_FADE_THRESHOLD = 100;

        let numRows, numCols, allNeighbors = [], revealInterval, lastRevealTime = 0, dynamicFadeRate = AMT_FADE_PER_FRAME;

        p.setup = function() {
          let section = document.getElementById(sectionId);
          if (!section) return;
          let cnv = p.createCanvas(section.offsetWidth, section.offsetHeight);
          cnv.parent(sectionId);
          cnv.style("position", "absolute");
          cnv.style("top", "0");
          cnv.style("left", "0");
          cnv.style("width", section.offsetWidth + "px");
          cnv.style("height", section.offsetHeight + "px");
          cnv.style("z-index", "-1");
          cnv.style("pointer-events", "none");
          p.strokeWeight(1);
          numRows = Math.ceil(section.offsetHeight / CELL_SIZE);
          numCols = Math.ceil(section.offsetWidth / CELL_SIZE);
          revealInterval = setInterval(attemptReveal, REVEAL_INTERVAL);
        };

        p.draw = function() {
          p.clear();
          if (allNeighbors.length > DYNAMIC_FADE_THRESHOLD) {
            dynamicFadeRate = AMT_FADE_PER_FRAME * (1 + (allNeighbors.length - DYNAMIC_FADE_THRESHOLD) / 50);
          } else {
            dynamicFadeRate = AMT_FADE_PER_FRAME;
          }
          for (let neighbor of allNeighbors) {
            let nx = neighbor.col * CELL_SIZE;
            let ny = neighbor.row * CELL_SIZE;
            neighbor.opacity = Math.max(0, neighbor.opacity - dynamicFadeRate);
            p.stroke(255, 255, 255, neighbor.opacity * (STARTING_STROKE_ALPHA / 100));
            p.fill(255, 255, 255, neighbor.opacity * (STARTING_FILL_ALPHA / 100));
            p.rect(nx, ny, CELL_SIZE, CELL_SIZE);
          }
          allNeighbors = allNeighbors.filter(n => n.opacity > 0);
        };

        function attemptReveal() {
          if (allNeighbors.length < MAX_CELLS) {
            let row = Math.floor(p.random(numRows));
            let col = Math.floor(p.random(numCols));
            let capacityRatio = allNeighbors.length / MAX_CELLS;
            let maxNew = Math.max(1, Math.floor(8 * (1 - capacityRatio)));
            allNeighbors.push(...getRandomNeighbors(row, col, maxNew));
          }
        }

        function getRandomNeighbors(row, col, maxN) {
          let pot = [];
          for (let dr = -1; dr <= 1; dr++) {
            for (let dc = -1; dc <= 1; dc++) {
              let nr = row + dr, nc = col + dc;
              if ((dr !== 0 || dc !== 0) && nr >= 0 && nr < numRows && nc >= 0 && nc < numCols) {
                pot.push({ row: nr, col: nc, opacity: 100 });
              }
            }
          }
          for (let i = pot.length - 1; i > 0; i--) {
            const j = Math.floor(Math.random() * (i + 1));
            [pot[i], pot[j]] = [pot[j], pot[i]];
          }
          return pot.slice(0, Math.min(maxN, pot.length));
        }

        p.windowResized = function() {
          let section = document.getElementById(sectionId);
          if (!section) return;
          p.resizeCanvas(section.offsetWidth, section.offsetHeight);
          numRows = Math.ceil(section.offsetHeight / CELL_SIZE);
          numCols = Math.ceil(section.offsetWidth / CELL_SIZE);
        };
      });
    }

    document.addEventListener("DOMContentLoaded", function() {
      createGridReveal('section_wrapper_1');
      createGridReveal('section_wrapper_2');
    });
  </script>

</BaseLayout>
