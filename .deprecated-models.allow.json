{
  "_doc": "Intentional references to deprecated model names that should NOT trigger findings in `pnpm check:models`. Two ways: phrases (regex, line-scoped) and files (whole-file exemption; an entry ending in `/` exempts a directory). Benchmark board records are exempt: each is a measurement taken while its models were live, and a model being deprecated later does not invalidate it.",
  "phrases": [
    "NEVER use",
    "Never use",
    "never use",
    "deprecated",
    "DO NOT USE",
    "Do not use",
    "anti-reference",
    "warning against",
    "claude-sonnet-5 · plan",
    "const MODELS = "
  ],
  "files": [
    "scripts/check-models.mjs",
    "scripts/visual-audit.mjs",
    "src/data/benchmarks/boards/",
    "scripts/fixtures/benchmark-board.json"
  ]
}
