{
  "_doc": "Intentional references to deprecated model names that should NOT trigger findings in `pnpm check:models`. Two ways: phrases (regex, line-scoped) and files (whole-file exemption). Benchmark result JSON is exempt: those are real historical measurements — a model being deprecated later does not invalidate a score measured while it was live.",
  "phrases": [
    "NEVER use",
    "Never use",
    "never use",
    "deprecated",
    "DO NOT USE",
    "Do not use",
    "anti-reference",
    "warning against"
  ],
  "files": [
    "scripts/check-models.mjs",
    "scripts/visual-audit.mjs",
    "src/data/benchmarks/results/tax.json",
    "src/data/benchmarks/results/tax-harness.json",
    "src/data/benchmarks/results/tax-steered.json",
    "src/data/benchmarks/results/tax-all.json"
  ]
}
