{
  "_doc": "Intentional references to deprecated model names that should NOT trigger findings in `pnpm check:models`. Two ways: phrases (regex, line-scoped) and files (whole-file exemption). Benchmark run records are exempt: those are real historical measurements, and a model being deprecated later does not invalidate a score measured while it was live.",
  "phrases": [
    "NEVER use",
    "Never use",
    "never use",
    "deprecated",
    "DO NOT USE",
    "Do not use",
    "anti-reference",
    "warning against",
    "claude-sonnet-5 · plan",
    "const MODELS = "
  ],
  "files": [
    "scripts/check-models.mjs",
    "scripts/visual-audit.mjs",
    "src/data/benchmarks/results/tax.json",
    "src/data/benchmarks/results/stripe.json",
    "src/data/benchmarks/results/calcom.json",
    "src/data/benchmarks/results/plausible.json",
    "src/data/benchmarks/results/posthog.json",
    "src/data/benchmarks/results/svix.json",
    "src/data/benchmarks/results/directus.json"
  ]
}
