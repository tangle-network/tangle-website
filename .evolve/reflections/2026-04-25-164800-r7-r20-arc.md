---
date: 2026-04-25
session: tangle-website R7 → R20 arc
grade: 6/10
duration: ~6 hours, 14 commits
---

# Reflect — tangle-website R7→R20 arc

## Run grade: 6/10

| Dimension | Score | Evidence |
|---|---|---|
| Goal achievement | 7/10 | 14 commits, 7 axe criticals → 0, ExamplePanel + sticky scroller killed, orbital diagram shipped, hero full-page + off-center fix, nav consolidation, real outcome metrics. Drew's verdict went from 3/10 to "still squished" — visual bar not met. |
| Code quality | 7/10 | Net delete-heavy (-461/+231 in R14). Section/BentoCell/CtaBlock primitives composing across 8 pages. 2 self-introduced regressions (vault flip white-on-white R16→R20; preserveAspectRatio CSS bug R17). |
| Efficiency | 4/10 | Burned 13 rounds on the same surface. Drew kept catching things I missed: duplicate eyebrow ("Secure Infrastructure for AI" pill above identical headline) lived 4 rounds. |
| Self-correction | 6/10 | Backed out global var rebind same round when over-rotation showed in audit. But shipped vault flips R16 without auditing class-based contrast — broke 4 pages, didn't notice until R20. |
| Learning | 6/10 | 2 memory entries, S-tier playbook with 50 techniques mapped, design-it tool spec. Did not capture the bigger lesson — see below. |

## Recurring flow (this is what consumed the session)

```
Drew screenshots problem  →  I find file  →  edit  →  restart dev  →  Drew screenshots next problem
~12 cycles, 5-15 min each, REACTIVE
```

Drew is doing the visual QA loop I should be doing. The fix is a calibrated judge or sub-agent that does it instead of me.

## Plateau analysis

- audit gpt-5.5: 7.0 stable across all 9 pages
- Drew: 3/10 → "still squished"

**The audit metric and the operator's verdict diverged sharply and never converged.** gpt-5.5 rates against generic "polished marketing page" template (eyebrow + headline + lede + CTAs + alternating sections). My code conforms. Drew wants what the audit can't see: visual originality, density, brand voice, creative direction.

The S-tier playbook I wrote at R17 nailed the gap (top-3 missing: customer logos + named outcomes, hero entrance choreography, spring physics). But I never executed against it — reactive cycle held me to surface fixes.

## Lessons (going to memory)

1. **Variant rebinds need blast-radius check.** Two over-rotations this session (R16 vault flip white-on-white, R20 var rebind dark-on-dark in nested dark components). Don't ship a "single-line fix everywhere" without auditing nested-component edge cases.

2. **Counting headlines are a tell.** "Three primitives / Four moves / Five lines" is a copywriting tic, not a hook. Drew killed all three in one breath at R19. Watch for it next time.

3. **Audit metric ≠ operator verdict** when the audit is gpt-5.5-tier. A consistent 7.0 with consistent 3/10 from Drew means stop optimizing the proxy. Sub-agent screenshot review or `/pursue` generation, not another round of /evolve.

4. **Reactive iteration costs more than parallel rebuild.** 13 rounds of CSS edits ≈ 1 round of "4 proposers rewrite from scratch" if I'd dispatched /multi-pursue at the R12 plateau. Should have called the play earlier.

## Action items (ranked)

1. **Dispatch `/multi-pursue` as already configured** at `.evolve/multi-pursue/config.json`. 4 worktree proposers each rewrite `/home` in Linear / Vercel / Stripe / Modal direction. Compose winner. THE path to S-tier.
2. **Customer logos + outcome metrics** — the biggest S-tier gap per the playbook. Needs Drew's input on which 4–6 teams + outcome lines.
3. **Build design-it MVP** (`~/company/tools/design-it/`). With it every future visual moment ships senior-grade by default. Saves the next 13 reactive rounds.
4. **Multi-rep audit harness** — 3 reps median in `bad-batch.sh`. Drops single-rep ±1 variance.
5. **Drop /evolve for tangle-website until /multi-pursue ships.** Iterating gpt-5.5's score from 7.0 is negative-EV.

## Dispatch (mandatory)

**Next: run `/multi-pursue`** with config at `/Users/drew/webb/tangle-website/.evolve/multi-pursue/config.json`. Baseline already taken (median-of-3 gpt-5.5 = 7.0 on `/`). 4 proposers, each given a different reference-site brief, each writes a complete `src/pages/index.astro`. Eval = bad-batch + Drew screenshot review.

NOT `/evolve`. /evolve has plateaued at 7.0 with this judge for 8 rounds. Continuing it is the same reactive-iteration trap that consumed this session.
